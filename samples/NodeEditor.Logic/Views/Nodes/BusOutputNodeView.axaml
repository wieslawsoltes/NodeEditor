<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:NodeEditorLogic.ViewModels.Nodes"
             xmlns:controls="clr-namespace:NodeEditor.Controls;assembly=NodeEditorAvalonia"
             mc:Ignorable="d" d:DesignWidth="180" d:DesignHeight="120"
             x:Class="NodeEditorLogic.Views.Nodes.BusOutputNodeView"
             x:CompileBindings="True" x:DataType="vm:LogicBusOutputNodeViewModel"
             ClipToBounds="False">
  <Design.DataContext>
    <vm:LogicBusOutputNodeViewModel Title="Bus Output" Label="BUS" BusWidth="4" ObservedText="0b1001" />
  </Design.DataContext>
  <Border Classes="logic-node">
    <StackPanel Spacing="6">
      <controls:EditableTextBlock Classes="logic-inline" Text="{Binding Title}" />
      <controls:EditableTextBlock Classes="logic-inline" Text="{Binding Label}" Width="90" />
      <StackPanel Orientation="Horizontal" Spacing="6">
        <TextBlock Text="Width" VerticalAlignment="Center" FontSize="11" />
        <NumericUpDown Classes="logic-inline" Minimum="1" Maximum="16" Width="60" Value="{Binding BusWidth}" />
      </StackPanel>
      <Border Classes="logic-led" Background="{DynamicResource LogicUnknownBrush}" />
      <TextBlock Text="{Binding ObservedText}" FontSize="12" />
    </StackPanel>
  </Border>
</UserControl>
